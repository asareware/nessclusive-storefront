{%- comment -%}
  The "+" glyph on a product card. Its own snippet purely so the add-to-cart
  and choose-options branches of ness-product-card cannot drift apart — the
  two controls must be visually identical.
{%- endcomment -%}
<svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" aria-hidden="true" focusable="false">
  <path d="M7 1.5V12.5M1.5 7H12.5"/>
</svg>
