{
  "name": "nessclusive-instagram-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Mirrors the five most recent Instagram posts into Shopify Files and a shop metafield the theme renders",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "sync": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
