{%- comment -%}
  Collection template.

  A .liquid template with a static {% section %} tag rather than
  templates/collection.json, for the same reason as the homepage: a JSON
  template lets the customizer reorder and delete sections, and the shop page's
  structure is fixed by the design. See PLAN.md, Decision #12.
{%- endcomment -%}

{% section 'ness-collection' %}
